version: "2"

services:

  cvolume:
    container_name: cvolume
    image: busybox
    volumes:
      - .:/app

  webserver-service:
    image: bitnami/nginx
    ports:
      - 80:80
    volumes:
      - ./containers-config/site.conf:/bitnami/nginx/conf/vhosts/site.conf
    volumes_from:
      - cvolume

  php-service:
    image: bitnami/php-fpm
    volumes_from:
      - cvolume

  db-service:
    image: bitnami/mariadb
    ports:
      - 3306:3306
    environment:
      - MARIADB_DATABASE=app
      - MARIADB_USER=app
      - MARIADB_PASSWORD=app
      - MARIADB_ROOT_PASSWORD=app
    volumes:
      - db-volume

volumes:
  db-volume:
    driver: local
